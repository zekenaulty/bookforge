# Run Phase - Scene Commit

Use this skill when a request maps to `run.commit`.

Required params
- `request`
- `book_id`
- `input_payload`

Optional params
- `chapter`
- `scene`
- `lint_report`

Output
- a deterministic commit checklist or commit-ready response
